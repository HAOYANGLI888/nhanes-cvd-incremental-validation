# Temporal Validation Lock

NHANES 2021-2023 is locked for final temporal validation only. It is not used for feature selection, missing-data strategy selection, hyperparameter tuning, threshold selection, recalibration, feature transformation fitting, age-range selection, BMI/waist decisions, or primary metric selection. The lock is represented in `config/temporal_validation_locked.yml`.
